# Product Roadmap

| v0.5.0 |
| -------- | 
| - introduce UI forms for epic and ticket (PRD mode) |
| - introduce markdown builder features to convert UI forms into markdown |
| - integrate UI forms with attachment (which will auto store the attachements into resource folder) |