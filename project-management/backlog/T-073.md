# T-073:

**Epic:** EPIC-016

## Description
- Create settings schema in SQLite for non-sensitive data
- Store preferences: theme, sidebar state, default editor mode
- API key stored in keyring only (not SQLite)
- Load settings on app startup
- Update settings on change

## Acceptance Criteria
- Settings table created
- Non-sensitive settings in SQLite
- API key never in SQLite
- Settings load on startup
- Updates persist

## Technical Notes
- Table: settings (key, value, updated_at)
- JSON values for complex settings
- Use tauri-plugin-sql
- Settings service/store in frontend
- Migration support

## Dependencies
- T-070

## Testing
- Settings save correctly
- Load on startup works
- Updates persist
- No API key in DB
